<%= form_for(@history_record) do |f| %>
  <% if @history_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@history_record.errors.count, "error") %> prohibited this history_record from being saved:</h2>

      <ul>
      <% @history_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :http_api_address %><br>
    <%= f.text_area :http_api_address %>
  </div>
  <div class="field">
    <%= f.label :fields_to_store %><br>
    <%= f.text_area :fields_to_store %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.text_field :rate %>
  </div>
  <div class="field">
    <%= f.label :public %><br>
    <%= f.check_box :public %>
  </div>
  <div class="field">
    <%= f.label :historian_id %><br>
    <%= f.collection_select :historian_id , Historian.all, :id, :id%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
